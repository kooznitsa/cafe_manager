name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install make
        run: |
          sudo apt-get update
          sudo apt-get -y install make

      - name: Create .env
        run: cp .env.sample .env

      - name: Create auth.json
        run: cp auth.json.sample auth.json

      - name: Run containers
        run: make run

  unit_test-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Lint files
        run: make linter

      - name: Run unit tests
        run: make unittest
